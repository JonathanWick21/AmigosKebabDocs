---
sidebar_position: 1
title: Tarjeta de Producto
---

# Tarjeta de Producto (`ReusableCard`)

El componente `ReusableCard` es la pieza fundamental del catálogo de **Amigos Kebab**. Está diseñado siguiendo principios de **usabilidad y accesibilidad** para garantizar que todos los usuarios puedan consultar nuestro menú de forma clara y sencilla.

## 📝 Descripción General
Este componente presenta la información visual y descriptiva de un producto de forma aislada, facilitando su reutilización en diferentes partes de la aplicación como el listado principal o la sección de administración.

### ♿ Características de Accesibilidad (RA 4)
Para cumplir con los criterios de accesibilidad exigidos, se han implementado las siguientes medidas:
- **Navegación por teclado**: La tarjeta es enfocable y navegable mediante el teclado gracias al uso de `tabIndex="0"`.
- **Semántica HTML5**: Se utiliza la etiqueta `<article>` para definir el contenido como una unidad independiente.
- **Lectores de pantalla**: Se han incluido etiquetas `aria-label` dinámicas que describen el producto y texto oculto para imágenes mediante clases `sr-only`.
- **Legibilidad**: El diseño utiliza tipografías y contrastes de color (basados en el rojo corporativo `#b3001b`) que aseguran una lectura óptima.

---

## 🚀 Ejemplos de Uso

### Implementación en Código
Así es como puedes declarar el componente dentro de tu aplicación de React:

```jsx
<ReusableCard 
  nombre="Dürum Grande" 
  precio={6.00} 
  foto="/img/durumGrande.jpg"
>
  Delicioso kebab en rollo con carne mixta, verduras frescas y salsa de la casa.
</ReusableCard>
```
Como parte de la guía de referencia técnica, puedes interactuar con el componente real en sus diferentes estados utilizando este panel de Storybook integrado. Esto permite verificar el comportamiento del componente de forma aislada.

<iframe src="http://localhost:6006/iframe.html?id=components-reusablecard--default&viewMode=story" width="100%" height="550px" style={{border: '2px solid #b3001b', borderRadius: '12px', marginTop: '1.5rem', boxShadow: '0 4px 12px rgba(0,0,0,0.1)'}}></iframe>

:::tip Ayuda al Usuario
Si la imagen no carga, el componente mostrará un estado alternativo manteniendo la estructura para no romper la interfaz de usuario.
:::

## 🛠️ Referencia de la API (Props)

| Propiedad | Tipo | Requerido | Descripción |
|-----------|------|-----------|-------------|
| nombre | string | Sí | Nombre comercial del producto que aparecerá en el encabezado. |
| precio | number | Sí | Valor numérico. El componente añade automáticamente el símbolo €. |
| foto | string | Sí | Ruta de la imagen. Incluye un efecto de zoom al pasar el ratón (hover). |
| children | node | No | Texto descriptivo opcional que se muestra bajo el precio. |